<section>
    {{!-- Display info about each streaming service --}}
    <ol>
        {{#each services as |service| }}
        <li>
            <div>
                Name: {{service.name}}
            </div>
            <div>
                Cost: {{service.cost}}
            </div>
        {{/each}}
    </ol>
</section>